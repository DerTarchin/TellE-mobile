HOW TO RUN:

To run locally, you simply need Django installed, and comment out the last line of settings.py

To run on Heroku, you simply need to use this directory to add to a new Heroku repo and link it to a Postgres database. Will work fine.

Otherwise, visit the site (live) by using this link: 
http://telle-mobile.herokuapp.com/